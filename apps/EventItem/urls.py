from django.urls import path
from . import views

urlpatterns = [
    #path('', views.event_item_list, name='event_item_list'),
    #path('<int:pk>/', views.event_item_detail, name='event_item_detail'),
]
