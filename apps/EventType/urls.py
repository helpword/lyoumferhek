from django.urls import path
from . import views

urlpatterns = [
    #path('', views.event_type_list, name='event_type_list'),
    #path('<int:pk>/', views.event_type_detail, name='event_type_detail'),
]
