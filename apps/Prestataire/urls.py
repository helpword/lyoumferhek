from django.urls import path
from . import views

urlpatterns = [
    #path('', views.prestataire_list, name='prestataire_list'),
    #path('<int:pk>/', views.prestataire_detail, name='prestataire_detail'),
]
