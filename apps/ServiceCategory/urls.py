from django.urls import path
from . import views

urlpatterns = [
    # path('', views.service_category_list, name='service_category_list'),
    # path('<int:pk>/', views.service_category_detail, name='service_category_detail'),
    # path('service-categories/', include('apps.servicecategory.urls')),

]

